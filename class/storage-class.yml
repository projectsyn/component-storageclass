parameters:
  kapitan:
    compile:
      - input_paths:
          - storage-class/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - storage-class/component/main.jsonnet
        input_type: jsonnet
        output_path: storage-class/
