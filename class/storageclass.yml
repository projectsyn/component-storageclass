parameters:
  kapitan:
    compile:
      - input_paths:
          - storageclass/component/app.jsonnet
        input_type: jsonnet
        output_path: .
      - input_paths:
          - storageclass/component/main.jsonnet
        input_type: jsonnet
        output_path: storageclass/
